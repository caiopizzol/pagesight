pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{ts,tsx}"
      run: bunx biome check --write {staged_files} && git add {staged_files}
    - name: typecheck
      run: bunx tsc --noEmit
